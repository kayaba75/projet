{% extends 'base-backend.html.twig' %}

{% block title %}Modifier Services{% endblock %}

{% block body %}
        <a href="{{ path('app_admin_services_index') }}" class="btn btn-dark mb-2">Retour à la liste</a>

    <div class="d-flex justify-content-between align-items-center">

    <h1>Modifier Services </h1>
    
    </div>

    {{ include('admin_services/_form.html.twig', {'button_label': 'Modifier'}) }}


    {{ include('admin_services/_delete_form.html.twig') }}
{% endblock %}
