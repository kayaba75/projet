{% extends 'base.html.twig' %}

{% block title %}Contactez-nous!{% endblock %}

{% block body %}
<h1>Contact</h1>
{{ form_start(form) }}
{# si l'utilisateur est connecté remplir la valeur de form.email avec l'email de l'utilisateur #}
{% if app.user %}

    {{ form_row(form.email, {'value': app.user.email}) }}
{% else %}
    {{ form_row(form.email) }}
{% endif %}
    {{ form_row(form.sujet) }}
    {{ form_row(form.message) }}
    {# {{ form_widget(form) }} #}
{{ form_end(form) }}


{% endblock %}
