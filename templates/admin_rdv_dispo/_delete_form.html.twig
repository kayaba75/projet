<form method="post" action="{{ path('app_admin_rdv_dispo_delete', {'id': rdv_dispo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rdv_dispo.id) }}">
    <button class="btn">Delete</button>
</form>
